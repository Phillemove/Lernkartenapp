<Window x:Class="De.HsFlensburg.ClientApp101.Ui.Desktop.ManageCard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:De.HsFlensburg.ClientApp101.Ui.Desktop"
        mc:Ignorable="d"
        Title="ManageCard" Height="828.55" Width="1015.5">
    <Window.DataContext>
        <Binding Source="{StaticResource VMLocator}" Path="cardWindowViewModel"></Binding>
    </Window.DataContext>
    <Grid Margin="0,0,40.6,49">
        <Label Margin="0,0,-20.8,726.2">All Boxes
        </Label>
        <DataGrid ItemsSource="{Binding Path=MyBoxesViewModel}" IsReadOnly="True"  
                HorizontalAlignment="Left" Height="392" 
                VerticalAlignment="Top" Width="968" 
                RenderTransformOrigin="0.5,0.5"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,24,0,0"/>
    </Grid>
</Window>
